tunnel: whatsup-earth
credentials-file: /root/.cloudflared/cert.pem
logfile: /var/log/cloudflared.log
loglevel: info

ingress:
  - hostname: {{ api_domain }}
    service: http://localhost:5000
  - hostname: {{ app_domain }}
    service: http://localhost:3000
  - service: http_status:404
